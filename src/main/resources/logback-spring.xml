<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Property for log directory - will be set dynamically by the application -->
    <property name="LOG_DIR" value="${LOG_DIR:-logs/run_default}" />
    
    <!-- Console appender for standard output -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File appender for complete console log -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_DIR}/console.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>
    
    
    <!-- Comprehensive application log - combines console and debug output -->
    <appender name="APPLICATION_LOG" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_DIR}/application.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="APPLICATION_LOG" />
    </root>
    
    <!-- Application-specific logger -->
    <logger name="com.media.sort" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="APPLICATION_LOG" />
    </logger>
    
    <!-- Spring Framework logger (less verbose) -->
    <logger name="org.springframework" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="APPLICATION_LOG" />
    </logger>
    
    <!-- Spring Batch logger -->
    <logger name="org.springframework.batch" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="APPLICATION_LOG" />
    </logger>
</configuration>
